/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['htmlburger.com'],  // Add the domain you are using for external images
    },
  };
  
  export default nextConfig;
  